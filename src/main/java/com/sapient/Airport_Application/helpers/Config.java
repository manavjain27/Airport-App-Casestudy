package com.sapient.Airport_Application.helpers;

public enum Config {
    AIRPORTS_CSV,
    REGIONS_CSV,
    NAVAIDS_CSV,
    COUNTRIES_CSV,
    RUNWAYS_CSV,
    AIRPORT_FREQUENCY_CSV,
    SECRET_KEY
}
